export enum PageAction {
  add,
  edit,
  cancel,
  clear,
  getData,
  getChildren,
}

export enum PageType {
  add,
  edit,
  view,
  // hold,
  // duplicate,
  // change
}

export enum SchedulePageType {
  add,
  edit,
  view,
  hold,
  duplicate,
  change,
  stop,
}
export enum PromoCodePageType {
  add,
  edit,
  duplicate,
  view,
  editAfterDublicate,
}

// export enum providerStatus {
//   Active = 1,
//   Pending = 2,
//   Approved = 3,
//   Rejected = 4,
//   Published = 5,
//   Suspended = 6,
// }
